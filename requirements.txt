# Core dependencies (cross-platform)
numpy>=1.19.0
scipy>=1.5.0
matplotlib>=3.3.0

# Audio processing (cross-platform)
sounddevice>=0.4.4
soundfile>=0.10.3

# Platform-specific audio libraries
# PyAudio - supports Windows, macOS, Linux
pyaudio>=0.2.11

# SimpleAudio - supports Windows, macOS, Linux
simpleaudio>=1.0.4
